# Only needed for Streamlit Community Cloud deployment - paste these into
# the app's Settings -> Secrets in the Streamlit Cloud UI, don't commit a
# real secrets.toml. Local development reads the same values from ../.env
# instead (see dashboard/streamlit_app.py's get_secret() helper).

SNOWFLAKE_ACCOUNT = "xy12345.us-east-1"
SNOWFLAKE_USER = "TRADE_ETL_SVC"
SNOWFLAKE_PASSWORD = "changeme"
SNOWFLAKE_ROLE = "TRADE_ETL_ROLE"
SNOWFLAKE_WAREHOUSE = "TRADE_ETL_WH"
SNOWFLAKE_DATABASE = "TRADE_ETL_DB"
